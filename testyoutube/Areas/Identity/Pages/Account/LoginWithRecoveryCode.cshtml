@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Vérification du code de récupération";
}

<div class="neumorphism">
    <h1>@ViewData["Title"]</h1>
    <hr />
    <p>
        Vous avez demandé à vous connecter avec un code de récupération. Cette connexion ne sera pas mémorisée tant que vous n'aurez pas fourni
        un code d'authentification lors de la connexion ou si vous désactivez 2FA et que vous vous connectez à nouveau.
    </p>
    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Input.RecoveryCode">Code de récupération</label>
                    <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                    <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary">Se connecter</button>
            </form>
        </div>
    </div>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
